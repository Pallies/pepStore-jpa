package fr.diginamic.utils.enums;

/**
 * The Enum ModeDB.
 * action of persistence
 */
public enum ModeDB {
        
        /** The persist. */
        PERSIST,
        
        /** The merge. */
        MERGE,
        
        /** The remove. */
        REMOVE;
}
